<Window x:Class="AbakConfigurator.SessionWindows"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{DynamicResource l_userSession}" Height="370" Width="412"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Loaded="windowLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <DataGrid x:Name="usersGrid" Grid.Row="1"  ColumnWidth="Auto" AutoGenerateColumns="True" IsReadOnly="True" MouseUp="usersGrid_MouseUp" >        
        </DataGrid>
    </Grid>
</Window>
